Place external model weights here.

Expected default path for the image backbone:

- `weights/vit_small_patch14_reg4_dinov2.lvd142m/model.safetensors`

You can also override this path with:

- `CLOVER_IMAGE_BACKBONE_WEIGHTS`
